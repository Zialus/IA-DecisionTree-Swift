language: generic
env: SWIFT_VERSION=5.2.3

jobs:
  include:
    - os: linux
      dist: xenial
      name: Ubuntu 16.04
    - os: linux
      dist: bionic
      name: Ubuntu 18.04
    - os: osx
      osx_image: xcode11.3
      name: Mojave
    - os: osx
      osx_image: xcode11.4
      name: Catalina

install:
  - eval "$(curl -sL https://swiftenv.fuller.li/install.sh)"

script:
  - make
  - make test
